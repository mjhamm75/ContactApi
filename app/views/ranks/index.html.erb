<h1>Listing ranks</h1>

<table>
  <tr>
    <th>Org</th>
    <th>Contact type cd</th>
    <th>Rank</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ranks.each do |rank| %>
  <tr>
    <td><%= rank.org_id %></td>
    <td><%= rank.contact_type_cd %></td>
    <td><%= rank.rank %></td>
    <td><%= link_to 'Show', rank %></td>
    <td><%= link_to 'Edit', edit_rank_path(rank) %></td>
    <td><%= link_to 'Destroy', rank, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Rank', new_rank_path %>
